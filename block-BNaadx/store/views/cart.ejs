<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css"
      rel="stylesheet">
</head>
<body>
    <header class="p-8 text-center bg-blue-400 text-4xl text-white flex justify-between">
        <h1>
            Customized Clothing
        </h1>
        <h2>
            <a href="/admin/Logout">
                <button>Logout</button>
            </a>
        </h2>
    </header>
    <main >
        <div class="flex justify-center flex-wrap">
   <% user.cart.forEach(product => { %>
    <div class="w-12 h-12">
        <img  src="<%= product.image %>" alt="productimage" width="100%" height="100%">
        <h3><%= product.name %></h3>
        <p>price:<%= product.price %></p>
        <span><%= product.quantity ? product.quantity: 0 %></span>
        <a href="/products/<%= product.id %>/quantity">
       <button>Quantity</button>
        </a>
        <h4>Description:<%= product.description%></h4>
      
        <a href="/products/<%= product._id %>/remove"> remove</a>

    </div>


   <% }) %>
                
        </div>
    </main>
</body>
</html>