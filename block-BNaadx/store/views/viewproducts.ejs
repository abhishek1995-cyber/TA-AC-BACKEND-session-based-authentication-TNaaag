<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css"
      rel="stylesheet">
</head>
<body>
    <header class="p-8 text-center bg-blue-400 text-4xl text-white flex justify-between">
        <h1>
            Customized Clothing
        </h1>
        <h2>
            <a href="/admin/Logout">
                <button>Logout</button>
            </a>
        </h2>
    </header>
    <main >
        <div class="flex justify-center flex-wrap">
            <% products.forEach(product=>{ %>
                <div class="w-32 h-32 text-center mt-12 mr-12">
                    <img  src="<%= product.image %>" alt="productimage" width="100%" height="100%">
                    <h3 ><%= product.name %></h3>
                    <a href="/products/<%= product._id %>">
                        <button class="border-solid border border-gray-700 bg-red-300">View Details</button>
                    </a>
                </div>
           <% }) %>
        </div>
    </main>
</body>
</html>